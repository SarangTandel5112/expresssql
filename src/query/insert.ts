class insert {
    public inserttry = () => {
        try {
            return 'insert into `Try` (`Name`,`Age`,`Roll`,`Address`) VALUES(?,?,?,?)'
        } catch (error) {

        }
    }
}

export default insert;